[package]
name = "bijjou"
version = "0.1.0"
edition = "2021"

[dependencies]
toml = "1.1"

[dev-dependencies]
insta = "1"
assert_cmd = "2"

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"
strip = "symbols"

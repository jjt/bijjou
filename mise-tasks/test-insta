#!/usr/bin/env bash
#MISE description="Run insta golden snapshot tests"
set -euo pipefail
# Prefer `cargo insta test` so a follow-up `cargo insta review` works smoothly.
if command -v cargo-insta >/dev/null 2>&1; then
  exec cargo insta test --test golden "$@"
fi
exec cargo test --test golden "$@"
